# TODO

- XenoNet: drop the old Phase I site labels (`1.h`, `2.3`). Forest 0.6.0 reports 0-based indexes; `phase1_site_strings` / `get_prob_for_one_site` still walk 1-based `N.h` / `a.b` Class-row keys.
