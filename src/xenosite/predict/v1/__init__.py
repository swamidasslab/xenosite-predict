"""Production predictors (scoring version ``"1"``, principled mapping).

Same ONNX weights as scoring version ``"0"``. Runner defaults are RDKit
symmetry, principled site/OMP/NRings. Version ``"0"`` is a legacy overlay
registered by :mod:`xenosite.predict.v1.legacy` (also imported as
:mod:`xenosite.predict.v0`, which warns).
"""
